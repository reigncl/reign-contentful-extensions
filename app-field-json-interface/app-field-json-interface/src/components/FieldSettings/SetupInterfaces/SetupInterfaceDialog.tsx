import { SetupInterfaceDialogProps } from "./SetupInterfaces.types";

const SetupInterfaceDialog = ({ sdk }: SetupInterfaceDialogProps) => {
  return <>SetupInterfaceDialog</>;
};

export default SetupInterfaceDialog;
